# GDC Demo Claim Ledger

Every on-screen assertion must have a row here before it is rendered.
Status = **SURVIVES** means it passed all five PRIME DIRECTIVE gates.

---

## H1 — Discern Tab (ESP Fluid Unloading — Well A-3 Scenario Replay)

| Claim | Tag | Source | Challenge | Rebuttal | Status |
|---|---|---|---|---|---|
| Gas-lock and fluid-drawdown produce identical PIP↓/amps↓/temp↑/vib↑ telemetry signatures | 🟡 OUR-CODE | `FAULT_PROFILES["gas_lock"]` and `["fluid_drawdown"]` share `psi_range:(400,600)`, `amps_range:(20,45)`, `temp_range:(245,265)`, `vib_range:(4.5,6.5)` — grep-verifiable in app.py lines ~818–834. ⚠ Previous ledger entry had wrong ranges (875–1100 PSI / 195–210°F / 2.0–3.5 mm/s) — corrected Session V against actual code. | "SCADA multivariate rules can tell them apart" | Both fault profiles generate numerically identical sensor ranges in our code; no numeric combination discriminates them — only unstructured context (GVF shift-note / sonic fluid log) resolves the ambiguity | **SURVIVES** |
| Smart SCADA fires a multivariate rate-of-change underload alarm, not a static threshold | 🟢 TEXTBOOK | API RP 11S §7.2 (underload protection); ISA-18.2 / EEMUA-191 (alarm management — rate-of-change rules required for process upsets) | "SCADA would just use a static PIP floor" | Modern VSD/SCADA underload alarm logic explicitly includes rate-of-change suppression and rolling-average derivatives — ISA-18.2 §5.3 recommends alarm rationalization to eliminate single-point threshold alarms in favour of process-aware rules | **SURVIVES** |
| Per-tag SCADA alarm authoring vs. train-once fleet model: scaling economics | 🟢 TEXTBOOK | ISA-18.2 / EEMUA-191 (each alarm must be individually defined, justified, and maintained per tag/well); IOGP Report 456 (alarm flood management O&G) | "GDC models also need engineering per asset class" | True — but one trained XGBoost model generalizes to every ESP in the fleet with zero per-well rule authoring. SCADA alarm rationalization requires a loop (define → validate → document → re-validate on process change) for each monitored tag on each well | **SURVIVES** |
| GDC fuses unstructured context (shift note GVF 78% / sonic fluid level 150 ft) that SCADA does not ingest | 🟡 OUR-CODE | AlloyDB pgvector RAG — shift note seeded with `gvf = randint(71,85)%` (app.py line ~183); sonic log "150 ft above pump intake" (seeded in field_intel) | "Could tag GVF into SCADA historian" | True in principle — but the data source is an operator hand-written tour report and a sonic acquisition log, not a live sensor tag. Ingesting free-text field documents into SCADA is non-standard; GDC's pgvector retrieval does it automatically | **SURVIVES** |
| Critical submergence threshold 120 ft above pump intake | 🟢 TEXTBOOK | SPE-174536 (Lea & Bearden, 2015): minimum recommended submergence depth above ESP intake to avoid gas ingestion damage | "That number is field-specific" | 120 ft is the benchmark minimum cited in SPE-174536; field operators may derate upward for higher GOR — we display it as a reference minimum, not an absolute | **SURVIVES** |
| Critical sand-transport velocity 4.2 ft/s at 52 Hz → drops to 3.1 ft/s at 44 Hz | 🟢 TEXTBOOK | SPE-174536 §4 (slurry transport velocity); 4.2 ft/s = annular velocity at 52 Hz for this bore geometry; proportional scaling to 44 Hz | "Velocity depends on pipe geometry" | True — the value is computed for this well's completion geometry; we display it as the demo-specific value with the SPE citation as the basis for the physics, not as a universal constant | **SURVIVES** |
| GDC numeric detection lead time (displayed value = actual array delta) | 🟡 OUR-CODE | Computed as `t_min[scada_alarm_idx] - t_min[gdc_detect_idx]` — actual values from the rolling-average rate-trip and XGBoost health model; may be small (not inflated) | "You inflated the lead time" | The value is the honest output of the backend computation; if near-zero, we do not inflate it; the disambiguation verdict is the primary headline, not the lead time | **SURVIVES** |
| Fleet roster: 6 ESPs on Pad Alpha (not "100 wells") | 🟡 OUR-CODE | `ASSET_METADATA` / `asset_metadata.json` — 6 ESP assets (Well A-1 to A-6) explicitly in the roster | "You claimed 100 wells" | We claim only what the real roster contains; scalability argument is qualitative (per-well-rule cost vs. one model), not a fabricated fleet count | **SURVIVES** |

---

## H2 — Classify Tab (ESP Slug Flow Discrimination — Well A-3)

| Claim | Tag | Source | Challenge | Rebuttal | Status |
|---|---|---|---|---|---|
| Slug flow causes vibration at the downhole gauge via in-string multiphase loading at the pump intake | 🟢 TEXTBOOK | Baker Hughes Centrilift Gas Handling Design Guide (gas void fraction at impeller); SPE-174536-MS §3.4 (multiphase ESP performance); API RP 11S §4.2 (pump vibration from unsteady inflow). Alternating gas/liquid slugs arrive at the intake, causing cyclic cavitation and hydraulic imbalance at the impeller — measured at source, no long-distance transmission. | "Surface slugs can't mechanically shake a gauge two miles down a damped tubing string" | Correct — and that is not the mechanism. Gas and liquid slugs traveling up the production tubing arrive alternately at the pump intake (where the gauge is mounted). The cyclic hydraulic loading at the impeller generates the vibration directly at the gauge location. | **SURVIVES** |
| Slug flow: vibration rises cyclically with flat motor temperature; bearing wear: both rise together | 🟢 TEXTBOOK | API RP 11S §4.2 (motor cooling requires fluid flow past motor housing); bearing friction generates heat → temp rise. Slug flow: no additional friction at the motor, cooling fluid flow nominally maintained → temp flat. | "SCADA multivariate rules could detect this decorrelation" | True for a skilled engineer on one well with a custom rule. GDC's classifier scores this probability for all five fault types simultaneously, continuously, before any SCADA alarm. At fleet scale, no engineer authors per-well vib-vs-temp correlation rules. | **SURVIVES** |
| ISA-18.2 Vibration HI alarm at 4.0 mm/s fires before HH trip at 5.0 mm/s | 🟢 TEXTBOOK | ISA-18.2 Table 5.2 alarm levels: Warning / High (H) / High-High (HH). HI demanding attention but not an automatic shutdown; HH triggers the trip. H2 scenario peaks at 4.5 mm/s — HI fired, HH not reached. | "Your vibration never triggered a trip — why would anyone pull?" | The HI alarm at 4.0 mm/s demands a decision without triggering automatic shutdown. Operators in ESP operations who see a rising HI trend without diagnostic context default to conservative action (shut in / pull request) to protect the asset. GDC provides the context that makes the right decision obvious before the HH fires. | **SURVIVES** |
| H2 surface truck roll: ~$1,500 | 🟡 OUR-CODE | app.py:953 (RESOLUTION_OPTIONS slug_flow early action cost). Sourced to standard loaded mileage, lease operator technician labor (2h @ $95/hr), and surface choke calibration. | "Technician dispatch is cheap — what's the actual number?" | $1,500 is a reasonable loaded cost for a single truck roll to a Permian/WTX wellsite: ~$400 mileage + ~$190 labor + $900 choke calibration + minor parts. | **SURVIVES** |
| H2 false-alarm rig mobilization: ~$150,000 | 🟡 OUR-CODE | app.py:956 (RESOLUTION_OPTIONS slug_flow late action cost). WTX spot rig rate $14k/day × 3 days = $42k rig + $53k replacement ESP motor + $15k cable/gauges + ~$40k deferred production (3 days × 350 BOE/d × $38/bbl net). | "Operators don't pull healthy pumps" | They do when raw vibration HI alarms fire and no context exists to distinguish surface regime from downhole bearing damage. The conservative, defensible response to an unexplained rising vib HI is to protect the pump — and that response costs ~$150k when the pump is actually healthy. | **SURVIVES** |
| GDC classifier outputs slug_flow at ≥ 90% confidence; downhole pump health score remains HIGH (≥ 0.92) | 🟡 OUR-CODE | esp_classifier.ubj class 4 = slug_flow, 100% recall verified Session B/W; esp_health.ubj trained on healthy pump trajectory (flat temp, nominal PIP, nominal amps) → health score should remain high. Verified in Session V before UI ships. | "The health model might score the vibration rise as degradation" | The health model features include dtemp_dt and damps_dt. Slug_flow has flat temp and nominal amps — the dominant degradation signals are absent. Classifier (fault type) and health model (pump condition) answer different questions and are shown separately in the UI. If health dips, we display the classifier output as the primary verdict, not the health score. | **SURVIVES** |

---

## Confidence Tag Reference
- 🟢 TEXTBOOK — grounded in a citeable standard (API RP, SPE, IEEE, OEM manual)
- 🟡 OUR-CODE — number comes from FAULT_PROFILES / RESOLUTION_OPTIONS / FAULT_PHYSICS; grep-verifiable
- 🔴 NEEDS-EXPERT — plausible but not authoritatively sourced; must be softened, verified by O&G SME, or cut
