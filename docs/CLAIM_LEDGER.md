# GDC Demo Claim Ledger

Every on-screen assertion must have a row here before it is rendered.
Status = **SURVIVES** means it passed all five PRIME DIRECTIVE gates.

---

## H1 — Discern Tab (ESP Fluid Unloading — Well A-3 Scenario Replay)

| Claim | Tag | Source | Challenge | Rebuttal | Status |
|---|---|---|---|---|---|
| Gas-lock and fluid-drawdown produce identical PIP↓/amps↓/temp↑/vib↑ telemetry signatures | 🟡 OUR-CODE | `FAULT_PROFILES["gas_lock"]` and `["fluid_drawdown"]` share `psi_range:(400,600)`, `amps_range:(20,45)`, `temp_range:(245,265)`, `vib_range:(4.5,6.5)` — grep-verifiable in app.py lines ~818–834. ⚠ Previous ledger entry had wrong ranges (875–1100 PSI / 195–210°F / 2.0–3.5 mm/s) — corrected Session V against actual code. | "SCADA multivariate rules can tell them apart" | Both fault profiles generate numerically identical sensor ranges in our code; no numeric combination discriminates them — only unstructured context (GVF shift-note / sonic fluid log) resolves the ambiguity | **SURVIVES** |
| Smart SCADA fires a static rolling-average underload floor (API RP 11S §7.2) at 1,020 PSI — GDC's XGBoost crosses its calibrated probability threshold ~5–8 min earlier via pre-threshold multivariate pattern scoring | 🟢 TEXTBOOK | API RP 11S §7.2 (underload protection setpoint); ISA-18.2 §5.3 (rolling-average window preferred over single-sample); `scada_rule_fired` in API response — verified 12/12 live runs fire the static floor | "You said SCADA fires rate-of-change — but your API says 'Static underload floor: rolling avg PIP < 1,020 PSI' every single time" | Correct observation. The code implements all three rules (rate, floor, undercurrent) and fires at the earliest. In practice the static floor (1,020 PSI ≈ 15% below nominal) is always crossed first because the ramp trajectory hits it before the rate alarm accumulates 3 sustained windows. We do NOT claim rate-of-change fires — we claim GDC detects ~5–8 min before whichever rule fires. Lead-time claim remains honest. | **SURVIVES (reworded Session AE)** |
| Per-tag SCADA alarm authoring vs. train-once fleet model: scaling economics | 🟢 TEXTBOOK | ISA-18.2 / EEMUA-191 (each alarm must be individually defined, justified, and maintained per tag/well); IOGP Report 456 (alarm flood management O&G) | "GDC models also need engineering per asset class" | True — but one trained XGBoost model generalizes to every ESP in the fleet with zero per-well rule authoring. SCADA alarm rationalization requires a loop (define → validate → document → re-validate on process change) for each monitored tag on each well | **SURVIVES** |
| GDC fuses unstructured context (shift note GVF 78% / sonic fluid level 150 ft) that SCADA does not ingest | 🟡 OUR-CODE | AlloyDB pgvector RAG — shift note seeded with `gvf = randint(71,85)%` (app.py line ~183); sonic log "150 ft above pump intake" (seeded in field_intel) | "Could tag GVF into SCADA historian" | True in principle — but the data source is an operator hand-written tour report and a sonic acquisition log, not a live sensor tag. Ingesting free-text field documents into SCADA is non-standard; GDC's pgvector retrieval does it automatically | **SURVIVES** |
| Critical submergence threshold 120 ft above pump intake | 🟢 TEXTBOOK | SPE-174536 (Lea & Bearden, 2015): minimum recommended submergence depth above ESP intake to avoid gas ingestion damage | "That number is field-specific" | 120 ft is the benchmark minimum cited in SPE-174536; field operators may derate upward for higher GOR — we display it as a reference minimum, not an absolute | **SURVIVES** |
| Critical sand-transport velocity 4.2 ft/s at 52 Hz → drops to 3.1 ft/s at 44 Hz | 🟢 TEXTBOOK | SPE-174536 §4 (slurry transport velocity); 4.2 ft/s = annular velocity at 52 Hz for this bore geometry; proportional scaling to 44 Hz | "Velocity depends on pipe geometry" | True — the value is computed for this well's completion geometry; we display it as the demo-specific value with the SPE citation as the basis for the physics, not as a universal constant | **SURVIVES** |
| GDC numeric detection lead time (displayed value = actual array delta) | 🟡 OUR-CODE | Computed as `t_min[scada_alarm_idx] - t_min[gdc_detect_idx]` — actual values from the rolling-average rate-trip and XGBoost health model; may be small (not inflated) | "You inflated the lead time" | The value is the honest output of the backend computation; if near-zero, we do not inflate it; the disambiguation verdict is the primary headline, not the lead time | **SURVIVES** |
| Fleet roster: 6 ESPs on Pad Alpha (not "100 wells") | 🟡 OUR-CODE | `ASSET_METADATA` / `asset_metadata.json` — 6 ESP assets (Well A-1 to A-6) explicitly in the roster | "You claimed 100 wells" | We claim only what the real roster contains; scalability argument is qualitative (per-well-rule cost vs. one model), not a fabricated fleet count | **SURVIVES** |

---

## H2 — Classify Tab (ESP Slug Flow Discrimination — Well A-3) — ❌ SCENARIO INVALIDATED SESSION AR

> Slug-flow H2 retired. All claims below are archived. Do not ship. Dual-AI red-team (Gemini + Claude Opus) found 4 FAILs: (1) temperature discriminator runs backwards; (2) false cost dichotomy; (3) deciding signal is telemetric; (4) APM understated. See DEMO_MASTER §5 and RED_TEAM_LEDGER.md for detail.

| Claim | Tag | Source | Challenge | Rebuttal | Status |
|---|---|---|---|---|---|
| Slug flow causes vibration at the downhole gauge via in-string multiphase loading at the pump intake | 🟢 TEXTBOOK | Baker Hughes Centrilift Gas Handling Design Guide; SPE-174536-MS §3.4; API RP 11S §4.2. | "Surface slugs can't mechanically shake a gauge two miles down" | Correct mechanism. | **ARCHIVED — scenario retired** |
| Slug flow: vibration rises cyclically with flat motor temperature | 🟢 TEXTBOOK | API RP 11S §4.2 | "Temperature discriminator is wrong — gas at intake reduces cooling, temp trends up" | ❌ FAIL — RTD is in motor, not pump/protector bearings. Discriminator runs backwards in high-GOR wells. | **ARCHIVED — FAIL** |
| H2 surface truck roll: ~$1,500 vs. false-alarm rig: ~$150k | 🟡 OUR-CODE | app.py:953/956 | "Real baseline is choke/VFD adjust first — same cheap action from the amp chart" | ❌ FAIL — false dichotomy. | **ARCHIVED — FAIL** |

---

## H2 — Classify Tab (ESP Maintenance Provenance — Workover Fluid Incompatibility) — ✅ ACTIVE SCENARIO

> Validated Session AT. All 4 survival tests pass. Gemini search confirmed API RP 11S3/11S5 ambiguity claim. Claim Ledger rows added Session AU.

| ID | Claim | Tag | Source | Challenge | Rebuttal | Status |
|---|---|---|---|---|---|---|
| H2-P1 | APM correctly identifies the bearing wear signature (amps elevated, vibration rising) — the bearing wear is real. It is caused by well fluid contaminating the bearing assembly through the degraded protector seal. APM cannot determine WHY the bearings are wearing without the workover completion report + OEM matrix. APM routes to: "bearing wear → pump pull" (correct symptom, wrong root cause, wrong fix). GDC fuses the documents, identifies incompatible workover fluid as root cause, recommends flush + reseal. | 🟢 TEXTBOOK | Failure sequence: Gemini search Session AV — seal degrades → pressure barrier lost → well fluid ingress → bearing contamination (multiple sources: MDPI ESP failure reviews, SLB, ChampionX). API RP 11S1 teardown classification. Session AV red-team: all 4 survival tests pass with strengthened Test 3 (APM misidentifies root cause, not symptom). | "APM would route to seal degradation, not bearing wear — it knows the difference" | Only with vibration spectral analysis or dedicated bearing-temp sensors (neither present on this standard 4-sensor string). On PIP + amps + winding temp + single-axis vib alone, the bearing contamination pattern is indistinguishable from normal bearing wear. APM routes to the statistically common hypothesis (normal wear) and recommends pull. | **SURVIVES (physics corrected Session AV — APM right symptom, wrong root cause)** |
| H2-P2 | Historical hydraulic fluid fill (during workover) is documented only in the workover completion report — no online sensor on the running ESP carries this information | 🟢 TEXTBOOK | Physical impossibility: no sensor on a running ESP measures what fluid was used during a prior workover intervention. The information originates as a human action recorded in a completion document. | "Oil analysis (fluid sampling) can detect incompatible fluid breakdown products" | Oil analysis requires: (a) a scheduled sample pull, (b) lab analysis, (c) days of turnaround. Not a real-time online sensor. Doesn't trigger in time. The workover completion report is the only contemporaneous, real-time-accessible record of what fluid was used. | **SURVIVES** |
| H2-P3 | Human factors / maintenance-provenance errors are common ESP failure contributors — the class is material, not edge-case | 🟡 OUR-CODE | 51% of ESP failures attributed to human factors/operational problems: 2014 SPE AI Conference survey; SPE 185275-MS (Camilleri et al.); SPE 194398-MS; SPE 144562-MS | "How often does a crew use the wrong fluid specifically?" | The claim is for the *class* (wrong fluid, non-spec parts, improper procedures, undocumented interventions) — not wrong-fluid exclusively. The class represents ~51% of failures. Wrong-fluid is a specific, common instance of the class. | **SURVIVES (scoped to class)** |
| H2-P4 | Symptom onset within weeks of incompatible hydraulic fluid fill — elastomer swell timeline consistent with Buna-N + phosphate ester exposure | 🟡 OUR-CODE | Gemini search (Session AV): (1) accelerated immersion standard = 168h / 7 days at 140°F shows noticeable Buna-N changes (15% swell threshold); (2) documented operational case: rubber particles in fluid after ~3 months (Buna-N + phosphate ester); (3) conclusion: "days to a few weeks" for initial symptoms, "few months" for system failure. Our onset (weeks after workover, before failure) falls within the documented range. Downhole temps (150–200°F typical Permian) may accelerate vs. lab ambient. | "Swell timelines are highly fluid- and temperature-dependent" | Confirmed by Gemini — temperature, fluid concentration, and elastomer grade all affect rate. Our scenario uses "within weeks of fluid fill" language (not a hard 3-week number), which is consistent with the cited range. The causal direction (workover → delayed onset → progressive degradation) is physically accurate and documented. | **SURVIVES (softened — display as "within weeks of fluid fill", not a hard number)** |
| H2-C1 | Cost of controlled flush + reseal: ~$8k–$15k | 🔴 NEEDS-EXPERT | Two Gemini searches (Session AU + AV): both confirmed no hard public figure for ESP seal section reseal in Permian Basin. Confirmed: (a) "substantially less than full rig workover"; (b) full workover AFE Andrews County WTX = $140k (Jul 2023); (c) avoiding a full pull "can reduce costs by 5–10% compared to traditional rig workovers" for one technology class. Specific reseal cost is proprietary and operator/OEM-specific. | "What's the actual cost? Could be more or less." | Display as soft estimate range ($8k–$15k) with explicit caveat. Two Gemini search passes could not source a hard number — this is the correct epistemic state. Frame in narration as "a fraction of the pull investigation cost" not as a precise figure. Hard display requires an actual field service quote from an ESP OEM or service company. | **SURVIVES (as soft range only) — 🔴 Cannot harden without field service quote** |
| H2-C2 | Cost of pump-pull investigation: ~$70k–$100k | 🟡 OUR-CODE | WTX spot rig rate ~$14k/day × 3 days = $42k + motor inspection + deferred production. Consistent with: $140k AFE for full ESP workover (Andrews County WTX, Jul 2023, Gemini search Session AU). Our range is the investigation subset (not full workover including replacement). | "Operators don't pull on early vibration alone — they monitor first" | Correct — and the scenario is scoped to the *investigation* decision after 3 weeks of trending degradation, not an immediate pull on day one. Investigation cost (mobilise pulling unit, pull string, inspect motor) is $70k–$100k even without replacement. Full replacement AFE: $140k+ per public record. | **SURVIVES (scoped to investigation subset)** |
| H2-D1 | Five H2 synthetic documents cover the full provenance chain (workover completion report + OEM fluid compatibility matrix + prior pull record + lease operator tour note + well history extract). All 5 passed G1–G6 gate. All Gemini red-team claims SURVIVES. Spec committed in docs/H2_SYNTHETIC_DOCS.md. | 🟡 OUR-CODE | docs/H2_SYNTHETIC_DOCS.md (Session AV) — G1–G6 gate table, Gemini second_opinion results per document. Physics: Gemini search Session AV (seal degradation → fluid ingress → bearing contamination). Chemistry: ASTM D471 (Buna-N + synthetic ester incompatibility). Motor IR: IEEE 43-2000. SCADA alarm realism: EEMUA 191. | "These documents are obviously synthetic" | G1: all vendors/OEMs are fictional. G2: all docs record observations only (no diagnosis). G3: no doc is alarming in isolation. G4: decisive only in combination. G5: all numbers physically consistent. G6: all provenance types realistic. Two docs (1 & 4) generated fresh by Gemma per run with randomized parameters. | **SURVIVES** |
| H2-D2 | Documents 1 (workover completion report) and 4 (tour note) are generated by Gemma per H2 scenario run with randomized vendor, product code, dates, operator initials, wellhead readings — each demo run produces unique document text | 🟡 OUR-CODE | docs/H2_SYNTHETIC_DOCS.md — Gemma prompt templates + randomized parameter ranges. Consistent with backend architecture: `/api/h2/scenario-replay` calls local Gemma to generate, injects parameters, returns in `doc_reveals[]` payload. | "The documents are pre-written text, not live AI generation" | Two documents are generated per-call by Gemma (local `gemma4:latest`). Vendor name, product code, dates, technician initials all randomized. Two consecutive demo runs produce different document text. Static docs (2, 3, 5) are Python date-templated at startup — dates auto-update, never hardcoded. | **SURVIVES** |

---

---

## H1 — Confidence & Discrimination (Session W additions)

| Claim | Tag | Source | Challenge | Rebuttal | Status |
|---|---|---|---|---|---|
| **REMOVED: `92% confidence` gas_lock literal** | — | index.html line 779 — was a hardcoded HTML string, no producer | "That's a fabricated number" | ❌ **INTEGRITY VIOLATION** — removed Session W Batch A. Replaced by Bayesian posterior. | **REMOVED** |
| **REMOVED: `94% confidence` fluid_drawdown literal** | — | index.html line 785 — was a hardcoded HTML string, no producer | "That's a fabricated number" | ❌ **INTEGRITY VIOLATION** — removed Session W Batch A. Replaced by Bayesian posterior. | **REMOVED** |
| GDC discrimination confidence = Bayesian posterior (naive-Bayes log-odds fusion over retrieved documents) | 🟢 method / 🔴 LR weights | Method: I. J. Good, *Probability and the Weighing of Evidence* (1950); T. J. Fagan, *NEJM* 293(5):257 (1975); Hastie/Tibshirani/Friedman *ESL* (2009) §6.6.3. LR values: conservative transparent weights grounded in API RP 11S §7.2 physics — labeled on-screen as weights, not calibrated parameters. Full methodology: docs/H1_METHODOLOGY.md | "The probability is made up — you can't compute diagnostic confidence from telemetry alone" | Correct — and by design. Prior = 50/50 (telemetry is ambiguous). Posterior moves to high-90s via document LRs (free gas absent, casing flat, fluid declining, GOR nominal). The math attributes 100% of discriminating power to the retrieved documents — exactly the L3 moat claim. Evidence table shown on-screen for full audit. | **SURVIVES** |
| H1 XGBoost detection: `esp_health.ubj` runs live `model.predict()` over the replay trajectory in a W=20 sliding window, producing real health scores | 🟡 OUR-CODE | app.py ~line 5870 — verified Session S (RMSE=0.00179). `model_used` field returned in API response. | "Your model is pre-recorded / hardcoded" | Trajectory freshly sampled each call (k∈[1.2,2.5], new baselines). `model.predict()` called 100× per run. Two runs are never identical. `model_used: esp_health.ubj` shown in response. | **SURVIVES** |
| H1 detection lead-time = `t_min[scada_alarm_idx] − t_min[gdc_detect_idx]` — live, not inflated | 🟡 OUR-CODE | app.py `h1_scenario_replay` — arithmetic on real array indices from XGBoost output vs ISA-18.2/API RP 11S SCADA rules. | "You inflated the lead time" | The value is the honest output; ~4–8 min typical; may be small on some runs; we do not floor it. The discrimination verdict (L3 moat) is the headline win, not the lead-time magnitude. | **SURVIVES** |

---

## APM Adoption & Competitive Positioning — Session BF

| ID | Claim | Tag | Source | Hostile Rebuttal | Our Answer | Status |
|---|---|---|---|---|---|---|
| **APM-D1** | ~5–15% of mid/small Permian independents run true multivariate ML PdM; the remainder (~85–95%) run threshold-only SCADA | 🔴 NEEDS-EXPERT | GlobalData Thematic Research: PdM in Oil & Gas; SPE maturity-matrix (SPE-219528 referenced); practitioner SME (Bill Barna, Permian production engineer, June 2026) — SUPPORTED verdict; disposable-ESP economics (100–300 day run-life) reduce incentive to capitalize ML | "That 5–15% is too low — look at all the AI case studies." | Vendor case studies are self-selected; independent ANALYST/ACADEMIC sources segment by operator size — supermajors adopt; mid/small independent penetration is low. Display as range with confidence band. Never a hard point. | **SURVIVES (as range, 🔴 tag displayed)** |
| **APM-D2** | Against threshold-only SCADA operators (~85–95% of target buyer population), XGBoost pre-threshold multivariate scoring is a genuine, honest detection edge | 🟢 TEXTBOOK / 🟡 OUR-CODE | API RP 11S SCADA alarm rules (ISA-18.2) define hard threshold logic; XGBoost scores joint multivariate drift continuously — architectural distinction not a speed claim. Scoped strictly to threshold-only tier. | "Any competent SCADA engineer adds rate-of-change alarms." | Conceded. Rate-of-change rules are single-sensor. XGBoost scores the joint correlated pattern across 4 sensors. Scope carefully: threshold-vs-pattern distinction, not sensitivity claim against best-of-breed APM. | **SURVIVES (scoped to threshold-only tier)** |
| **LIFTIQ-1** | GDC is architecturally designed for on-prem sovereign AI from the ground up; SLB Lift IQ was architected cloud-first and adapted for on-prem via Lift IQ Edge | 🔴 NEEDS-EXPERT | SLB Lift IQ Edge product existence confirmed (Gemini Test B, Session BF); "cloud-first adapted" characterization needs SLB architecture documentation to harden. Presenter framing point, not a hard technical claim. | "Lift IQ Edge keeps raw data on-premise — same sovereignty." | Acknowledged; reframe to architectural design philosophy and whose infrastructure manages the on-prem stack. For NOC buyers: GDC runs on operator-owned infrastructure. Verify deployment model before asserting. | **SURVIVES-IF-REWORDED (Ledger/presenter notes — not on screen)** |
| **LIFTIQ-2** | SLB has a *perceived* conflict of interest: recommends corrective actions while also selling the pump, workover, and pull services | 🔴 NEEDS-EXPERT | Structural observation (same-entity incentive misalignment); contracts may include performance guarantees that partially align incentives. Must soften to "perceived/potential." | "Our recommendations are driven by physics models; we have performance-guarantee contracts." | Acknowledged — incentive alignment concern is structural (same entity benefits from more pulls), not a claim of deliberate bias. Present as transparency advantage of vendor-neutral platform. | **SURVIVES-IF-REWORDED (presenter notes only)** |
| **LIFTIQ-3** | Lift IQ's *automated* diagnostic system does not fuse unstructured vendor emails, overdue-PM logs, and field documents into real-time diagnosis at fleet scale in seconds; human SLB experts may, but not automatically at 2am across 200 wells | 🟡 OUR-CODE | GDC L3 architecture (pgvector RAG, automated fleet-scale, < 2s) is verifiable from our codebase. "SLB experts may incorporate context" = conceded human workflow. "Not automated at fleet scale in seconds" = our architectural claim. | "Our domain experts regularly incorporate operational context from unstructured data sources." | Conceded — human experts do this. GDC's advantage is doing it *automatically*, across *every asset*, in *< 2 seconds*, at 2am, without requiring an on-call data scientist. Scale + automation + speed, not exclusive access. | **SURVIVES (with automated/fleet-scale scope)** |

---

## Confidence Tag Reference
- 🟢 TEXTBOOK — grounded in a citeable standard (API RP, SPE, IEEE, OEM manual)
- 🟡 OUR-CODE — number comes from FAULT_PROFILES / RESOLUTION_OPTIONS / FAULT_PHYSICS; grep-verifiable
- 🔴 NEEDS-EXPERT — plausible but not authoritatively sourced; must be softened, verified by O&G SME, or cut
